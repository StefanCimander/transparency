package com.transparency.entity

enum class SignalDirection {

    /** The signal is sent by the logical function. */
    SENDER,

    /** The signal is received by the logical function. */
    RECEIVER
}